{
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://smart-qr-order-api.onrender.com/api/:path*"
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
